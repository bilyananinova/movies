<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <link rel="icon" href="/static/images/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/css/style.css">
    <link href="https://use.fontawesome.com/releases/v5.0.6/css/all.css" rel="stylesheet">
    <title>Movies</title>
</head>

<body>
    <div class="container">
        <input type="checkbox" id="menu" class="menu">
        <header class="site-header">
            <label for="menu" class="menu-button">
                <span class="close">X</span>
                <span class="open">Menu</span>
            </label>
            <nav class="navigation">
            <h1><a class="home" href="/"></a></h1>
                <ul>
                    {{#if auth}}
                    <li class="welcome-msg"><a href="/my-page">Welcome, {{user.username}}</a></li>
                    <li><a href="/all-movies">All Movies</a></li>
                    <li><a href="/create">Add Movie</a></li>
                    <li><a href="/logout">Logout</a></li>
                    {{else}}
                    <li><a href="/login">Login</a></li>
                    <li><a href="/register">Register</a></li>
                    {{/if}}
                </ul>
            </nav>
        </header>

        <main class="site-main">
            {{#if error}}
            <div class="notification">
                <div class="errorBox">
                    <span>{{error}}</span>
                </div>
            </div>
            {{/if}}

            {{{body}}}
        </main>

        <footer class="site-footer">
            <p>@Movies: Exercise for Express.js and Handlebars</p>
        </footer>

    </div>
</body>

</html>